node_modules/
dist/
build/
.env
*.log
.DS_Store
Thumbs.db

# Runtime data (feedback DB, processing artifacts)
data/feedback.db
data/feedback.db-wal
data/feedback.db-shm
data/game-art/
processing/
*.bak

# Claude Code local artifacts (worktrees, local launch config, personal permission overrides)
.claude/worktrees/
.claude/launch.json
.claude/settings.local.json

# Scratch / temp working dirs
tmp/

# Python bytecode cache
__pycache__/

# Bundled FFmpeg binaries (#251) — too large for GitHub; populated per build
# machine by scripts/fetch-ffmpeg.ps1, shipped via extraResources.
vendor/ffmpeg/
vendor/beta-token.json

# AI engine runtime build tree + output zips (#146) — multi-GB; built by
# scripts/build-runtime.ps1, uploaded to R2, never committed.
vendor/runtime-build/
vendor/runtime-dist/

# Windows reserved-name junk (stray `> nul` redirects)
nul
