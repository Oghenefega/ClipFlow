---
description: Build React frontend and launch Electron app
---

Build and launch ClipFlow:

! npx react-scripts build 2>&1 | tail -5

If the build succeeded, launch the app:

! npm start &

Report whether the build succeeded or failed. If it failed, show the error and suggest a fix.
