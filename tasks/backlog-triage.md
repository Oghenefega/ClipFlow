# ClipFlow Backlog Triage — 2026-06-09 (session 73)

Generated by an 11-agent triage workflow that read every open code issue's body and cross-checked the code, then the close/rescope set in Section A was **independently re-verified by the session lead** — which corrected the #40/#108 specifics the agents got wrong (see below). 46 open code issues (launch/ops parked under `track: launch-ops`, excluded). This is a living menu — update as issues close.

---

## A. Verify-and-close or rescope (backlog is overstated)
Not open work — already fixed, mis-scoped, or partly stale. Acting on this section: **5 closeable now** (#112, #93, #64, #84, #10 → 46→41), **3 rescoped** (#85, #26, #32 — stay open but smaller/clearer), and **2 corrected but kept open** (#40, #108 — the agents' "just delete it" read was wrong).

> **✅ Executed 2026-06-09 (session 73):** 5 closed with `status: untested` (#112, #93, #64, #84, #10); 3 rescoped + commented (#85 & #32 retitled, #26); 2 corrected & kept open (#40, #108). Open code backlog **46 → 41**. Each close carries an evidence comment; re-open any that fails its in-app check (noted per issue).

| # | Action | Finding |
|---|---|---|
| #85 | **Rescope** | Core already shipped (verified): prompt extracted to `ai/title-caption-prompt.js` (`main.js:58`); `anthropic:rephraseOption`/`regenerateOption` IPC live (`main.js:2101-2102`, `preload.js:102-103`). Genuinely unbuilt (no code refs): peak-frame/visual context into the prompt, `clip.aiHistory` persistence + schema migration, history-nav UI (batch arrows). Not an epic. |
| #40 | **Keep open — small cleanup** | NOT a clean close. The issue body lists `CLUSTER_GAP_PX`/`CLUSTER_MIN_WIDTH_PX` as "dead", but they're **live** (imported `TimelinePanelNew.js:24`, used in `UploadView.js`) — do NOT delete. Re-verify each remaining item against current code before removing: `[DBG]` logs in `PreviewPanelNew.js`/`usePlaybackStore.js`, `commitAudioResize`/`commitLeftExtend`, `clip:extend`/`clip:extendLeft` IPCs, `MERGE_THRESHOLD`. |
| #108 | **Keep open — small refactor** | `rippleDeleteAudioSegment` confirmed 0 callers (def only, `useEditorStore.js:490`), but `audioSegments` is still **persisted on save** for back-compat — audit-then-remove (confirm no live reader, then drop the actions or quarantine the field), not an inline delete. |
| #112 | **Close** | Global EPIPE guard already at `main.js:22-27`; stated child-stdin cause is wrong. Optional per-stream hardening only. |
| #84 | **Verify + close** | Both write paths fixed + startup repair migration wired in `main.js`. Spot-check a few old `project.json` clips for in-range sub1, confirm migration ran. |
| #93 | **Verify + close** | Empty-delete branch already clears both models; 2 cited functions no longer exist. Confirm no caller leaves audioSegments/nleSegments divergent. |
| #10 | **Verify + close** | Likely fixed by NLE Phase 3/4 waveform rework (8227a3d, 5f408e8). Confirm a live trim reshapes the waveform. |
| #64 | **Close after #124** | -ar 1000 + 128MB maxBuffer, track-0 fallback, error propagation, waveformError state all present. Only the log-sink leftover (owned by #124). |
| #26 | **Rescope** | Account storage rewrite already done. Only per-account toggle/caption keying gap is real — small polish. |
| #32 | **Rescope** | yPercent-revert already fixed; only `capWidthPercent` persist gap remains (2-line). |
| #39 | **Carry separately** | Not a code defect — a 13-step manual QA checklist. Ongoing sign-off, not a fix. |

---

## B. Fix-first this week — safe, high-leverage (low regression risk)
1. **#124** — route waveform/ffmpeg `console.*` through `logger.*` (MODULES.videoProcessing). Unblocks all later diagnosis. _S_
2. **#92** — stop showing "Applied" when the silent save failed (false-success / data loss). #85 needs honest save. _S_
3. **#68** — move `energy_scorer.py` to `tools/`, `__dirname`-relative path, add to bundler/extraResources (cover `transcribe.py` too). Launch blocker. _S/M_
4. **#62** — silent-audio fallback (emit zero-energy JSON exit 0 + pipeline keyword/thirds fallback). Same file as #68; one verification pass on a normal + a silent clip. _M_
5. **#101** — add `punctuationRemove` to the restore mapping (dead branch). _S_
6. **#32** — restore `capWidthPercent` in `restoreSavedStyles()` (it restores `yPercent` only today — `useEditorStore.js:225`) and ensure it's persisted on save. _S_
7. **#106** — passive wheel listener: copy the correct pattern at `PreviewPanelNew.js:183-196`. _S_

Batch #101/#32/#106 (+#87 clamp) in one safe store-and-UI pass.

---

## C. Fragile zone — SOLO, own commit, verify on a GENERATED clip, never batch
The twice-reverted karaoke/subtitle area. **#131 is the keystone** (carry a stable `srcWordIdx` through `visibleWords`/`getActiveWordInSeg`/`handleWordClick`) — it unblocks #107 and de-risks #95.

Sequence:
1. **#89** — carry forward edited text on `setSegmentMode` rebuild (silent data loss). _M_
2. **#131** — `srcWordIdx` plumbing (highlight + click-seek wrong word after trim). _M_
3. **#132** — word-click-during-playback highlight freeze; rides #131's verification, own commit. _S_
4. **#107** — split-at-word wrong target (unblocked by #131's srcWordIdx). _M_
5. **#95** — splitSegment dup/drop word: single-predicate partition + clamp; removes its stale-index branch. _M_
6. **#99** — caption effects bleed across clips (custom template missing effect fields). reset() fix is WRONG. _M_
7. **#90 + #88** — clip-load playhead snaps on stale video time; gate on `readyState`. Re-read #88 (`_videoRef` outside set()) in the same pass. _M_
8. **#114** — preview-vs-editor wrap mismatch (same `setSegmentMode`/dedup path as #89; fold #121 comment fix in). _M_
9. **#105** — over-trim 0.1s sliver (needs Fega decision: auto-remove vs keep-floor; two divergent MIN_SEGMENT_DURATION constants). _M_
10. **#63** — sandbox offscreen subtitle BrowserWindow (rebundle overlay as classic-script shims, flip sandbox). Verify rendered subtitles. _L_

---

## D. Fix-together batches (shared root cause)
- **#68 → #62** — same `energy_scorer.py`; relocate before logic change.
- **#131 → #132 → #107 → #95** — filtered-vs-raw word-index desync; one `srcWordIdx` serves all (but each its own commit).
- **#89 → #114 (+#121)** — `setSegmentMode`/`originalSegments`/chunking path.
- **#90 + #88** — `usePlaybackStore._videoRef`.
- **#124 → #64** — same waveform log lines; #64 becomes verify-and-close.
- **#4 → #1 → #5** — ClipBrowser/ClipRow; fields already persist, no migration.
- **#14 → #9** — ProfileDiffModal accept flow; build editable panels before the toast.
- **#40 + #108** — Phase-4 hygiene + dead `audioSegments`; re-verify each item first (some "dead" constants are live — see Section A), then one cleanup commit.

---

## E. Park — roadmap, not backlog cleanup
- **Features:** #69 trim toggle (XL), #15 subtitle dictionary, #6 in-editor publish, #7 projects search, #4/#1/#5 publishing chain, #14/#9 learning-style, #70 rename flexibility, #73 splash (Phase 1 cheap; Phase 2 code-split later), #82 avatar cache.
- **Cosmetic / XL:** #86 icon system (gated on design decision), #127 info glyph, #128 scrub proxy.

---

## Bottom line
This week: lead with cheap integrity work — #124 (observability) → #92 (honest save) → #68→#62 (pipeline for real machines + silent clips) → batch the S-effort quick wins (#101, #32, #106). THEN enter the fragile karaoke zone one-per-commit, verified on a generated clip: #89 → #131 (+#132) → #99 → #90+#88. Park the XL features and cosmetics. Before all of it, close the 5 verified-resolved (#112/#93/#64/#84/#10), rescope #85/#26/#32, and correct #40/#108 (kept open — their "just delete it" specifics were stale) to make the backlog honest.
